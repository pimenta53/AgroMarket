<%= form_for(@price_type) do |f| %>
  <% if @price_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@price_type.errors.count, "error") %> proibiu este tipo de preço de ser salvado:</h2>

      <ul>
      <% @price_type.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
