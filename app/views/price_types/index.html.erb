<h1>Listing price_types</h1>

<table>
  <thead>
    <tr>
      <th>Name</th>
      <th></th>
      <th></th>
      <th></th>
    </tr>
  </thead>

  <tbody>
    <% @price_types.each do |price_type| %>
      <tr>
        <td><%= price_type.name %></td>
        <td><%= link_to 'Show', price_type %></td>
        <td><%= link_to 'Edit', edit_price_type_path(price_type) %></td>
        <td><%= link_to 'Destroy', price_type, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Price type', new_price_type_path %>
