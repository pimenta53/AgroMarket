
<% number_notifications_ads = @notifications_new_answer.length %>
<li class="dropdown messages-dropdown">

  <% if number_notifications_ads > 0 %>
    <a href="#" class="dropdown-toggle" data-toggle="dropdown"><i class="fa fa-book" id="fa-book"></i><span class="badge" id="custom-badge"><%= number_notifications_ads %></span><b class="caret" id="dropCarret"></b></a>
  <% else %>
    <a href="#" class="dropdown-toggle" data-toggle="dropdown"><i class="fa fa-book"  id="fa-book"></i><b class="caret" id="dropCarret"></b></a>
  <% end %>

  <ul class="dropdown-menu">

    <%# notifications new answers for your questions %>
    <% if @notifications_new_answer.length > 0 %>
      <li class="dropdown-header"><%= @notifications_new_answer.length %> Novas Respostas</li>
      <% @notifications_new_answer.each do |notification| %>
        <% question = Academy::Question.find(notification.id_destination) %>
        <li class="preview">
          <a href="<%= academy_question_path(question, :notification => notification) %>">
            <span class="name"><%= question.title %></span>
          </a>
        </li>
      <% end %>
      <li class="divider"></li>
    <% end %>

    <% if number_notifications_ads == 0 %>
      <li class="dropdown-header">Sem novos alertas.</li>
    <% end %>

  </ul>
</li>

