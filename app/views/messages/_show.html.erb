
<%# My messages%>
<% if message.user_sender == @ad.user_id %>
  <% valor = "div_adminMessage" %>
<% else %>
  <% if message.user_sender == current_user.id %>
    <% valor = "div_myMessage" %>
  <% else %>
    <% valor = "div_otherMessage" %>
  <% end %>
<%end%>
<div id="<%=valor%>">

  <p>
    <small>
      <%= message.created_at.strftime("%d-%m-%Y") %>
    </small>
  </p>

  <%= message.text %>

  <p hidden="true">
    <strong>is_read:</strong>
    <%= message.is_read %>
  </p>

</div>

