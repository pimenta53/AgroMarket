
<div class="workshop_inscription_title">

       <p> Lista de Registo do Workshop </p>

</div> 


<table class="table table-hover">
  <thead>
    <tr class="workshop_table_head">
      <th>Workshop</th>
      <th>Utilizador</th>
      <th>Registo em:</th>
      <th>Opcões</th>
    </tr>
  </thead>

  <tbody>
    <% @academy_workshop_registrations.each do |academy_workshop_registration| %>
      <tr>
        <td><%= @academy_workshop.title %></td>
        <td><%= academy_workshop_registration.user.name %></td>
        <td><%= academy_workshop_registration.created_at.strftime("%F") %></td>
        <td><%= link_to 'Apagar Inscricao', academy_workshop_registration, method: :delete, data: { confirm: 'Tens a certeza?' } %></td>
      </tr>
    <% end %>
  </tbody>

</table>

<br>


