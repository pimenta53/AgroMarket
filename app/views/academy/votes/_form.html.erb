<%= form_for(@academy_vote) do |f| %>
  <% if @academy_vote.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@academy_vote.errors.count, "error") %> prohibited this academy_vote from being saved:</h2>

      <ul>
      <% @academy_vote.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :vote %><br>
    <%= f.number_field :vote %>
  </div>
  <div class="field">
    <%= f.label :user_id %><br>
    <%= f.number_field :user_id %>
  </div>
  <div class="field">
    <%= f.label :answer_id %><br>
    <%= f.number_field :answer_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
